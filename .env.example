# This project is research-only and uses public GET endpoints.
# Do not place API keys, wallet mnemonics, or CLOB signing secrets here.
# Trading credentials are out of scope and must never be added.
